1. generr cratpeta siguales a las de src
dao/


importar dao
importar moongose
intalar el moludo de assert dende node
importar de sde mocha el describe y it



const assert=Assert.strict

conectar base de datos:
 
==que la de app, ver la base de datos que maneja el proyecto


describe("Pruebas dao users", function(){
    this.timeout(10000)//10seg
    //after, before??
    before(async function()=>{ //las funciones flecja no se puede uar el this.
        this.dao=new Users()
    })//crear uan instacia del dao
    it("el dao con su metodo get retorna un array de usuarios", async function(){
        let resultado= await this.dao.get
        //console.log(resultado)

        assert.equal(Array.isArray(resultado), true)// --> true --> positivo 
        //se puede hacer cualquier tipo de operacion js
        assert.equal()
    })//pruba concreta: nombre y funcion que se ejecuta la pruba

}) sirve para definor un entorno de trabajo

aftereach se ejecuta depues da cada pruba 
y before antes de cada prueba
it son los assert(afirmaciones) no sirven de nada 

pra jecutar: npx mocha ./test/**/*.js --exit




chai  --> instalar
importar expect de chai

el resto es igual a lo anterior 